library(latex2exp)
mean_try = rep(alpha.mean, nw)*x*(1-x)+(ifelse(0<rep(p.mean[i],nw),1,0))*
(ifelse(x<=rep(c.mean[i],nw),1,0))*N*x^2*rep(tau.mean[i],nw)*(1-rep(c.mean[i],nw))^2+
(ifelse(0<rep(p.mean[i],nw),1,0))*(ifelse(x>rep(c.mean[i],nw),1,0))*N*
(1-x)^2*rep(tau.mean[i],nw)*rep(c.mean[i],nw)^2
plot(0:N, S_sim[,i], xlab = "time", ylab = TeX(r'($Y_{T,k}$)'), type = "l", ylim = c(0, max(S_sim[,i], mean_try)),
lty = 1, lwd = 2)
lines(0:N, mean_try, col = "red", lty = 2, lwd = 2)
abline(v = c.mean[i]*N, col = "red", lty = 2, lwd = 2)
possible_change = seq(0, 1, 0.01)
possible_beta = seq(-1, -40, by = -0.01)
result_linear = matrix(0, ncol = 3, nrow = length(possible_beta)*length(possible_change))
colnames(result_linear) = c("squared error", "cp", "slope")
index = 1
for(ci in possible_change){
for(betai in possible_beta){
result_linear[index, 2:3] = c(ci, betai)
mean = betai*((ci-1)*x0+(x0-ci)*(ifelse(x0>ci,1,0)))
result_linear[index, 1] = sum((S_sim[, i] - mean)^2)
index = index + 1
}
}
cp_linear = result_linear[which.min(result_linear[, 1]), ][2]
beta_linear = result_linear[which.min(result_linear[, 1]), ][3]
x <- seq(0,1,0.02)
m_red = beta_linear*((cp_linear-1)*x+(x-cp_linear)*(ifelse(x>cp_linear,1,0)))
lines(0:N, m_red, col = 'green', lty = 3, lwd = 2)
abline(v = cp_linear*N, col = "green", lty = 3, lwd = 2)
abline(v = k_star[i]*N, col = "blue", lty = 4, lwd = 2)
legend('topright', legend = c(TeX(r'($Y_{T,k}$)'), 'Quadratic Fit', 'Linear Fit',
'True change point'),
col = c('black', 'red', 'green', 'blue'),
lty = c(1,2,3,4), cex = 1, bty = 'n')
library(latex2exp)
for (i in index_alt) {
N = nt = 50; x = (0:N)/N; nw = N+1
r_thin = seq(iters*3/4+1, iters, thin_step)
x <- seq(0,1,0.02)
library(latex2exp)
mean_try = rep(alpha.mean, nw)*x*(1-x)+(ifelse(0<rep(p.mean[i],nw),1,0))*
(ifelse(x<=rep(c.mean[i],nw),1,0))*N*x^2*rep(tau.mean[i],nw)*(1-rep(c.mean[i],nw))^2+
(ifelse(0<rep(p.mean[i],nw),1,0))*(ifelse(x>rep(c.mean[i],nw),1,0))*N*
(1-x)^2*rep(tau.mean[i],nw)*rep(c.mean[i],nw)^2
plot(0:N, S_sim[,i], xlab = paste("time", i), ylab = TeX(r'($Y_{T,k}$)'), type = "l", ylim = c(0, max(S_sim[,i], mean_try)),
lty = 1, lwd = 2)
lines(0:N, mean_try, col = "red", lty = 2, lwd = 2)
abline(v = c.mean[i]*N, col = "red", lty = 2, lwd = 2)
possible_change = seq(0, 1, 0.01)
possible_beta = seq(-1, -40, by = -0.01)
result_linear = matrix(0, ncol = 3, nrow = length(possible_beta)*length(possible_change))
colnames(result_linear) = c("squared error", "cp", "slope")
index = 1
for(ci in possible_change){
for(betai in possible_beta){
result_linear[index, 2:3] = c(ci, betai)
mean = betai*((ci-1)*x0+(x0-ci)*(ifelse(x0>ci,1,0)))
result_linear[index, 1] = sum((S_sim[, i] - mean)^2)
index = index + 1
}
}
cp_linear = result_linear[which.min(result_linear[, 1]), ][2]
beta_linear = result_linear[which.min(result_linear[, 1]), ][3]
x <- seq(0,1,0.02)
m_red = beta_linear*((cp_linear-1)*x+(x-cp_linear)*(ifelse(x>cp_linear,1,0)))
lines(0:N, m_red, col = 'green', lwd = 2, lty = 3)
abline(v = cp_linear*N, col = "green", lty = 3, lwd = 2)
abline(v = k_star[i]*N, col = "blue", lty = 4, lwd = 2)
}
i = 17
N = nt = 50; x = (0:N)/N; nw = N+1
r_thin = seq(iters*3/4+1, iters, thin_step)
x <- seq(0,1,0.02)
library(latex2exp)
mean_try = rep(alpha.mean, nw)*x*(1-x)+(ifelse(0<rep(p.mean[i],nw),1,0))*
(ifelse(x<=rep(c.mean[i],nw),1,0))*N*x^2*rep(tau.mean[i],nw)*(1-rep(c.mean[i],nw))^2+
(ifelse(0<rep(p.mean[i],nw),1,0))*(ifelse(x>rep(c.mean[i],nw),1,0))*N*
(1-x)^2*rep(tau.mean[i],nw)*rep(c.mean[i],nw)^2
plot(0:N, S_sim[,i], xlab = "time", ylab = TeX(r'($Y_{T,k}$)'), type = "l", ylim = c(0, max(S_sim[,i], mean_try)),
lty = 1, lwd = 2)
lines(0:N, mean_try, col = "red", lty = 2, lwd = 2)
abline(v = c.mean[i]*N, col = "red", lty = 2, lwd = 2)
possible_change = seq(0, 1, 0.01)
possible_beta = seq(-1, -40, by = -0.01)
result_linear = matrix(0, ncol = 3, nrow = length(possible_beta)*length(possible_change))
colnames(result_linear) = c("squared error", "cp", "slope")
index = 1
for(ci in possible_change){
for(betai in possible_beta){
result_linear[index, 2:3] = c(ci, betai)
mean = betai*((ci-1)*x0+(x0-ci)*(ifelse(x0>ci,1,0)))
result_linear[index, 1] = sum((S_sim[, i] - mean)^2)
index = index + 1
}
}
cp_linear = result_linear[which.min(result_linear[, 1]), ][2]
beta_linear = result_linear[which.min(result_linear[, 1]), ][3]
x <- seq(0,1,0.02)
m_red = beta_linear*((cp_linear-1)*x+(x-cp_linear)*(ifelse(x>cp_linear,1,0)))
lines(0:N, m_red, col = 'green', lty = 3, lwd = 2)
abline(v = cp_linear*N, col = "green", lty = 3, lwd = 2)
abline(v = k_star[i]*N, col = "blue", lty = 4, lwd = 2)
legend('topright', legend = c(TeX(r'($Y_{T,k}$)'), 'Quadratic Fit', 'Linear Fit',
'True change point'),
col = c('black', 'red', 'green', 'blue'),
lty = c(1,2,3,4), cex = 1, bty = 'n')
i = 17
N = nt = 50; x = (0:N)/N; nw = N+1
r_thin = seq(iters*3/4+1, iters, thin_step)
x <- seq(0,1,0.02)
library(latex2exp)
mean_try = rep(alpha.mean, nw)*x*(1-x)+(ifelse(0<rep(p.mean[i],nw),1,0))*
(ifelse(x<=rep(c.mean[i],nw),1,0))*N*x^2*rep(tau.mean[i],nw)*(1-rep(c.mean[i],nw))^2+
(ifelse(0<rep(p.mean[i],nw),1,0))*(ifelse(x>rep(c.mean[i],nw),1,0))*N*
(1-x)^2*rep(tau.mean[i],nw)*rep(c.mean[i],nw)^2
plot(0:N, S_sim[,i], xlab = "time", ylab = TeX(r'($Y_{T,k}$)'), type = "l", ylim = c(0, max(S_sim[,i], mean_try)),
lty = 1, lwd = 2)
lines(0:N, mean_try, col = "red", lty = 2, lwd = 2)
abline(v = c.mean[i]*N, col = "red", lty = 2, lwd = 2)
possible_change = seq(0, 1, 0.01)
possible_beta = seq(-1, -40, by = -0.01)
result_linear = matrix(0, ncol = 3, nrow = length(possible_beta)*length(possible_change))
colnames(result_linear) = c("squared error", "cp", "slope")
index = 1
for(ci in possible_change){
for(betai in possible_beta){
result_linear[index, 2:3] = c(ci, betai)
mean = betai*((ci-1)*x0+(x0-ci)*(ifelse(x0>ci,1,0)))
result_linear[index, 1] = sum((S_sim[, i] - mean)^2)
index = index + 1
}
}
cp_linear = result_linear[which.min(result_linear[, 1]), ][2]
beta_linear = result_linear[which.min(result_linear[, 1]), ][3]
x <- seq(0,1,0.02)
m_red = beta_linear*((cp_linear-1)*x+(x-cp_linear)*(ifelse(x>cp_linear,1,0)))
lines(0:N, m_red, col = 'green', lty = 3, lwd = 2)
abline(v = cp_linear*N, col = "green", lty = 3, lwd = 2)
abline(v = k_star[i]*N, col = "blue", lty = 4, lwd = 2)
legend('topleft', legend = c(TeX(r'($Y_{T,k}$)'), 'Quadratic Fit', 'Linear Fit',
'True change point'),
col = c('black', 'red', 'green', 'blue'),
lty = c(1,2,3,4), cex = 1, bty = 'n')
i = 17
N = nt = 50; x = (0:N)/N; nw = N+1
r_thin = seq(iters*3/4+1, iters, thin_step)
x <- seq(0,1,0.02)
library(latex2exp)
mean_try = rep(alpha.mean, nw)*x*(1-x)+(ifelse(0<rep(p.mean[i],nw),1,0))*
(ifelse(x<=rep(c.mean[i],nw),1,0))*N*x^2*rep(tau.mean[i],nw)*(1-rep(c.mean[i],nw))^2+
(ifelse(0<rep(p.mean[i],nw),1,0))*(ifelse(x>rep(c.mean[i],nw),1,0))*N*
(1-x)^2*rep(tau.mean[i],nw)*rep(c.mean[i],nw)^2
plot(0:N, S_sim[,i], xlab = "time", ylab = TeX(r'($Y_{T,k}$)'), type = "l", ylim = c(0, max(S_sim[,i], mean_try)),
lty = 1, lwd = 2)
lines(0:N, mean_try, col = "red", lty = 2, lwd = 2)
abline(v = c.mean[i]*N, col = "red", lty = 2, lwd = 2)
possible_change = seq(0, 1, 0.01)
possible_beta = seq(-1, -40, by = -0.01)
result_linear = matrix(0, ncol = 3, nrow = length(possible_beta)*length(possible_change))
colnames(result_linear) = c("squared error", "cp", "slope")
index = 1
for(ci in possible_change){
for(betai in possible_beta){
result_linear[index, 2:3] = c(ci, betai)
mean = betai*((ci-1)*x0+(x0-ci)*(ifelse(x0>ci,1,0)))
result_linear[index, 1] = sum((S_sim[, i] - mean)^2)
index = index + 1
}
}
cp_linear = result_linear[which.min(result_linear[, 1]), ][2]
beta_linear = result_linear[which.min(result_linear[, 1]), ][3]
x <- seq(0,1,0.02)
m_red = beta_linear*((cp_linear-1)*x+(x-cp_linear)*(ifelse(x>cp_linear,1,0)))
lines(0:N, m_red, col = 'green', lty = 3, lwd = 2)
abline(v = cp_linear*N, col = "green", lty = 3, lwd = 2)
abline(v = k_star[i]*N, col = "blue", lty = 4, lwd = 2)
legend('topleft', legend = c(TeX(r'($Y_{T,k}$)'), 'Quadratic Fit', 'Linear Fit',
'True change point'),
col = c('black', 'red', 'green', 'blue'),
lty = c(1,2,3,4), cex = 0.9, bty = 'n')
i = 20
N = nt = 50; x = (0:N)/N; nw = N+1
r_thin = seq(iters*3/4+1, iters, thin_step)
x <- seq(0,1,0.02)
library(latex2exp)
mean_try = rep(alpha.mean, nw)*x*(1-x)+(ifelse(0<rep(p.mean[i],nw),1,0))*
(ifelse(x<=rep(c.mean[i],nw),1,0))*N*x^2*rep(tau.mean[i],nw)*(1-rep(c.mean[i],nw))^2+
(ifelse(0<rep(p.mean[i],nw),1,0))*(ifelse(x>rep(c.mean[i],nw),1,0))*N*
(1-x)^2*rep(tau.mean[i],nw)*rep(c.mean[i],nw)^2
plot(0:N, S_sim[,i], xlab = "time", ylab = TeX(r'($Y_{T,k}$)'), type = "l", ylim = c(0, max(S_sim[,i], mean_try)),
lty = 1, lwd = 2)
lines(0:N, mean_try, col = "red", lty = 2, lwd = 2)
abline(v = c.mean[i]*N, col = "red", lty = 2, lwd = 2)
possible_change = seq(0, 1, 0.01)
possible_beta = seq(-1, -40, by = -0.01)
result_linear = matrix(0, ncol = 3, nrow = length(possible_beta)*length(possible_change))
colnames(result_linear) = c("squared error", "cp", "slope")
index = 1
for(ci in possible_change){
for(betai in possible_beta){
result_linear[index, 2:3] = c(ci, betai)
mean = betai*((ci-1)*x0+(x0-ci)*(ifelse(x0>ci,1,0)))
result_linear[index, 1] = sum((S_sim[, i] - mean)^2)
index = index + 1
}
}
cp_linear = result_linear[which.min(result_linear[, 1]), ][2]
beta_linear = result_linear[which.min(result_linear[, 1]), ][3]
x <- seq(0,1,0.02)
m_red = beta_linear*((cp_linear-1)*x+(x-cp_linear)*(ifelse(x>cp_linear,1,0)))
lines(0:N, m_red, col = 'green', lty = 3, lwd = 2)
abline(v = cp_linear*N, col = "green", lty = 3, lwd = 2)
abline(v = k_star[i]*N, col = "blue", lty = 4, lwd = 2)
legend('topleft', legend = c(TeX(r'($Y_{T,k}$)'), 'Quadratic Fit', 'Linear Fit',
'True change point'),
col = c('black', 'red', 'green', 'blue'),
lty = c(1,2,3,4), cex = 0.9, bty = 'n')
i = 20
N = nt = 50; x = (0:N)/N; nw = N+1
r_thin = seq(iters*3/4+1, iters, thin_step)
x <- seq(0,1,0.02)
library(latex2exp)
mean_try = rep(alpha.mean, nw)*x*(1-x)+(ifelse(0<rep(p.mean[i],nw),1,0))*
(ifelse(x<=rep(c.mean[i],nw),1,0))*N*x^2*rep(tau.mean[i],nw)*(1-rep(c.mean[i],nw))^2+
(ifelse(0<rep(p.mean[i],nw),1,0))*(ifelse(x>rep(c.mean[i],nw),1,0))*N*
(1-x)^2*rep(tau.mean[i],nw)*rep(c.mean[i],nw)^2
plot(0:N, S_sim[,i], xlab = "time", ylab = TeX(r'($Y_{T,k}$)'), type = "l", ylim = c(0, max(S_sim[,i], mean_try)),
lty = 1, lwd = 2)
lines(0:N, mean_try, col = "red", lty = 2, lwd = 2)
abline(v = c.mean[i]*N, col = "red", lty = 2, lwd = 2)
possible_change = seq(0, 1, 0.01)
possible_beta = seq(-1, -40, by = -0.01)
result_linear = matrix(0, ncol = 3, nrow = length(possible_beta)*length(possible_change))
colnames(result_linear) = c("squared error", "cp", "slope")
index = 1
for(ci in possible_change){
for(betai in possible_beta){
result_linear[index, 2:3] = c(ci, betai)
mean = betai*((ci-1)*x0+(x0-ci)*(ifelse(x0>ci,1,0)))
result_linear[index, 1] = sum((S_sim[, i] - mean)^2)
index = index + 1
}
}
cp_linear = result_linear[which.min(result_linear[, 1]), ][2]
beta_linear = result_linear[which.min(result_linear[, 1]), ][3]
x <- seq(0,1,0.02)
m_red = beta_linear*((cp_linear-1)*x+(x-cp_linear)*(ifelse(x>cp_linear,1,0)))
lines(0:N, m_red, col = 'green', lty = 3, lwd = 2)
abline(v = cp_linear*N, col = "green", lty = 3, lwd = 2)
abline(v = k_star[i]*N, col = "blue", lty = 4, lwd = 2)
legend('topright', legend = c(TeX(r'($Y_{T,k}$)'), 'Quadratic Fit', 'Linear Fit',
'True change point'),
col = c('black', 'red', 'green', 'blue'),
lty = c(1,2,3,4), cex = 0.9, bty = 'n')
i = 30
N = nt = 50; x = (0:N)/N; nw = N+1
r_thin = seq(iters*3/4+1, iters, thin_step)
x <- seq(0,1,0.02)
library(latex2exp)
mean_try = rep(alpha.mean, nw)*x*(1-x)+(ifelse(0<rep(p.mean[i],nw),1,0))*
(ifelse(x<=rep(c.mean[i],nw),1,0))*N*x^2*rep(tau.mean[i],nw)*(1-rep(c.mean[i],nw))^2+
(ifelse(0<rep(p.mean[i],nw),1,0))*(ifelse(x>rep(c.mean[i],nw),1,0))*N*
(1-x)^2*rep(tau.mean[i],nw)*rep(c.mean[i],nw)^2
plot(0:N, S_sim[,i], xlab = "time", ylab = TeX(r'($Y_{T,k}$)'), type = "l", ylim = c(0, max(S_sim[,i], mean_try)),
lty = 1, lwd = 2)
lines(0:N, mean_try, col = "red", lty = 2, lwd = 2)
abline(v = c.mean[i]*N, col = "red", lty = 2, lwd = 2)
possible_change = seq(0, 1, 0.01)
possible_beta = seq(-1, -40, by = -0.01)
result_linear = matrix(0, ncol = 3, nrow = length(possible_beta)*length(possible_change))
colnames(result_linear) = c("squared error", "cp", "slope")
index = 1
for(ci in possible_change){
for(betai in possible_beta){
result_linear[index, 2:3] = c(ci, betai)
mean = betai*((ci-1)*x0+(x0-ci)*(ifelse(x0>ci,1,0)))
result_linear[index, 1] = sum((S_sim[, i] - mean)^2)
index = index + 1
}
}
cp_linear = result_linear[which.min(result_linear[, 1]), ][2]
beta_linear = result_linear[which.min(result_linear[, 1]), ][3]
x <- seq(0,1,0.02)
m_red = beta_linear*((cp_linear-1)*x+(x-cp_linear)*(ifelse(x>cp_linear,1,0)))
lines(0:N, m_red, col = 'green', lty = 3, lwd = 2)
abline(v = cp_linear*N, col = "green", lty = 3, lwd = 2)
abline(v = k_star[i]*N, col = "blue", lty = 4, lwd = 2)
legend('topright', legend = c(TeX(r'($Y_{T,k}$)'), 'Quadratic Fit', 'Linear Fit',
'True change point'),
col = c('black', 'red', 'green', 'blue'),
lty = c(1,2,3,4), cex = 0.9, bty = 'n')
i = 37
N = nt = 50; x = (0:N)/N; nw = N+1
r_thin = seq(iters*3/4+1, iters, thin_step)
x <- seq(0,1,0.02)
library(latex2exp)
mean_try = rep(alpha.mean, nw)*x*(1-x)+(ifelse(0<rep(p.mean[i],nw),1,0))*
(ifelse(x<=rep(c.mean[i],nw),1,0))*N*x^2*rep(tau.mean[i],nw)*(1-rep(c.mean[i],nw))^2+
(ifelse(0<rep(p.mean[i],nw),1,0))*(ifelse(x>rep(c.mean[i],nw),1,0))*N*
(1-x)^2*rep(tau.mean[i],nw)*rep(c.mean[i],nw)^2
plot(0:N, S_sim[,i], xlab = "time", ylab = TeX(r'($Y_{T,k}$)'), type = "l", ylim = c(0, max(S_sim[,i], mean_try)),
lty = 1, lwd = 2)
lines(0:N, mean_try, col = "red", lty = 2, lwd = 2)
abline(v = c.mean[i]*N, col = "red", lty = 2, lwd = 2)
possible_change = seq(0, 1, 0.01)
possible_beta = seq(-1, -40, by = -0.01)
result_linear = matrix(0, ncol = 3, nrow = length(possible_beta)*length(possible_change))
colnames(result_linear) = c("squared error", "cp", "slope")
index = 1
for(ci in possible_change){
for(betai in possible_beta){
result_linear[index, 2:3] = c(ci, betai)
mean = betai*((ci-1)*x0+(x0-ci)*(ifelse(x0>ci,1,0)))
result_linear[index, 1] = sum((S_sim[, i] - mean)^2)
index = index + 1
}
}
cp_linear = result_linear[which.min(result_linear[, 1]), ][2]
beta_linear = result_linear[which.min(result_linear[, 1]), ][3]
x <- seq(0,1,0.02)
m_red = beta_linear*((cp_linear-1)*x+(x-cp_linear)*(ifelse(x>cp_linear,1,0)))
lines(0:N, m_red, col = 'green', lty = 3, lwd = 2)
abline(v = cp_linear*N, col = "green", lty = 3, lwd = 2)
abline(v = k_star[i]*N, col = "blue", lty = 4, lwd = 2)
legend('topright', legend = c(TeX(r'($Y_{T,k}$)'), 'Quadratic Fit', 'Linear Fit',
'True change point'),
col = c('black', 'red', 'green', 'blue'),
lty = c(1,2,3,4), cex = 0.9, bty = 'n')
i = 37
N = nt = 50; x = (0:N)/N; nw = N+1
r_thin = seq(iters*3/4+1, iters, thin_step)
x <- seq(0,1,0.02)
library(latex2exp)
mean_try = rep(alpha.mean, nw)*x*(1-x)+(ifelse(0<rep(p.mean[i],nw),1,0))*
(ifelse(x<=rep(c.mean[i],nw),1,0))*N*x^2*rep(tau.mean[i],nw)*(1-rep(c.mean[i],nw))^2+
(ifelse(0<rep(p.mean[i],nw),1,0))*(ifelse(x>rep(c.mean[i],nw),1,0))*N*
(1-x)^2*rep(tau.mean[i],nw)*rep(c.mean[i],nw)^2
plot(0:N, S_sim[,i], xlab = "time", ylab = TeX(r'($Y_{T,k}$)'), type = "l", ylim = c(0, max(S_sim[,i], mean_try)),
lty = 1, lwd = 2)
lines(0:N, mean_try, col = "red", lty = 2, lwd = 2)
abline(v = c.mean[i]*N, col = "red", lty = 2, lwd = 2)
possible_change = seq(0, 1, 0.01)
possible_beta = seq(-1, -40, by = -0.01)
result_linear = matrix(0, ncol = 3, nrow = length(possible_beta)*length(possible_change))
colnames(result_linear) = c("squared error", "cp", "slope")
index = 1
for(ci in possible_change){
for(betai in possible_beta){
result_linear[index, 2:3] = c(ci, betai)
mean = betai*((ci-1)*x0+(x0-ci)*(ifelse(x0>ci,1,0)))
result_linear[index, 1] = sum((S_sim[, i] - mean)^2)
index = index + 1
}
}
cp_linear = result_linear[which.min(result_linear[, 1]), ][2]
beta_linear = result_linear[which.min(result_linear[, 1]), ][3]
x <- seq(0,1,0.02)
m_red = beta_linear*((cp_linear-1)*x+(x-cp_linear)*(ifelse(x>cp_linear,1,0)))
lines(0:N, m_red, col = 'green', lty = 3, lwd = 2)
abline(v = cp_linear*N, col = "green", lty = 3, lwd = 2)
abline(v = k_star[i]*N, col = "blue", lty = 4, lwd = 2)
legend('topleft', legend = c(TeX(r'($Y_{T,k}$)'), 'Quadratic Fit', 'Linear Fit',
'True change point'),
col = c('black', 'red', 'green', 'blue'),
lty = c(1,2,3,4), cex = 0.9, bty = 'n')
i = 30
N = nt = 50; x = (0:N)/N; nw = N+1
r_thin = seq(iters*3/4+1, iters, thin_step)
x <- seq(0,1,0.02)
library(latex2exp)
mean_try = rep(alpha.mean, nw)*x*(1-x)+(ifelse(0<rep(p.mean[i],nw),1,0))*
(ifelse(x<=rep(c.mean[i],nw),1,0))*N*x^2*rep(tau.mean[i],nw)*(1-rep(c.mean[i],nw))^2+
(ifelse(0<rep(p.mean[i],nw),1,0))*(ifelse(x>rep(c.mean[i],nw),1,0))*N*
(1-x)^2*rep(tau.mean[i],nw)*rep(c.mean[i],nw)^2
plot(0:N, S_sim[,i], xlab = "time", ylab = TeX(r'($Y_{T,k}$)'), type = "l", ylim = c(0, max(S_sim[,i], mean_try)),
lty = 1, lwd = 2)
lines(0:N, mean_try, col = "red", lty = 2, lwd = 2)
abline(v = c.mean[i]*N, col = "red", lty = 2, lwd = 2)
possible_change = seq(0, 1, 0.01)
possible_beta = seq(-1, -40, by = -0.01)
result_linear = matrix(0, ncol = 3, nrow = length(possible_beta)*length(possible_change))
colnames(result_linear) = c("squared error", "cp", "slope")
index = 1
for(ci in possible_change){
for(betai in possible_beta){
result_linear[index, 2:3] = c(ci, betai)
mean = betai*((ci-1)*x0+(x0-ci)*(ifelse(x0>ci,1,0)))
result_linear[index, 1] = sum((S_sim[, i] - mean)^2)
index = index + 1
}
}
cp_linear = result_linear[which.min(result_linear[, 1]), ][2]
beta_linear = result_linear[which.min(result_linear[, 1]), ][3]
x <- seq(0,1,0.02)
m_red = beta_linear*((cp_linear-1)*x+(x-cp_linear)*(ifelse(x>cp_linear,1,0)))
lines(0:N, m_red, col = 'green', lty = 3, lwd = 2)
abline(v = cp_linear*N, col = "green", lty = 3, lwd = 2)
abline(v = k_star[i]*N, col = "blue", lty = 4, lwd = 2)
legend('topleft', legend = c(TeX(r'($Y_{T,k}$)'), 'Quadratic Fit', 'Linear Fit',
'True change point'),
col = c('black', 'red', 'green', 'blue'),
lty = c(1,2,3,4), cex = 1.5, bty = 'n')
i = 30
N = nt = 50; x = (0:N)/N; nw = N+1
r_thin = seq(iters*3/4+1, iters, thin_step)
x <- seq(0,1,0.02)
library(latex2exp)
mean_try = rep(alpha.mean, nw)*x*(1-x)+(ifelse(0<rep(p.mean[i],nw),1,0))*
(ifelse(x<=rep(c.mean[i],nw),1,0))*N*x^2*rep(tau.mean[i],nw)*(1-rep(c.mean[i],nw))^2+
(ifelse(0<rep(p.mean[i],nw),1,0))*(ifelse(x>rep(c.mean[i],nw),1,0))*N*
(1-x)^2*rep(tau.mean[i],nw)*rep(c.mean[i],nw)^2
plot(0:N, S_sim[,i], xlab = "time", ylab = TeX(r'($Y_{T,k}$)'), type = "l", ylim = c(0, max(S_sim[,i], mean_try)),
lty = 1, lwd = 2)
lines(0:N, mean_try, col = "red", lty = 2, lwd = 2)
abline(v = c.mean[i]*N, col = "red", lty = 2, lwd = 2)
possible_change = seq(0, 1, 0.01)
possible_beta = seq(-1, -40, by = -0.01)
result_linear = matrix(0, ncol = 3, nrow = length(possible_beta)*length(possible_change))
colnames(result_linear) = c("squared error", "cp", "slope")
index = 1
for(ci in possible_change){
for(betai in possible_beta){
result_linear[index, 2:3] = c(ci, betai)
mean = betai*((ci-1)*x0+(x0-ci)*(ifelse(x0>ci,1,0)))
result_linear[index, 1] = sum((S_sim[, i] - mean)^2)
index = index + 1
}
}
cp_linear = result_linear[which.min(result_linear[, 1]), ][2]
beta_linear = result_linear[which.min(result_linear[, 1]), ][3]
x <- seq(0,1,0.02)
m_red = beta_linear*((cp_linear-1)*x+(x-cp_linear)*(ifelse(x>cp_linear,1,0)))
lines(0:N, m_red, col = 'green', lty = 3, lwd = 2)
abline(v = cp_linear*N, col = "green", lty = 3, lwd = 2)
abline(v = k_star[i]*N, col = "blue", lty = 4, lwd = 2)
legend('topright', legend = c(TeX(r'($Y_{T,k}$)'), 'Quadratic Fit', 'Linear Fit',
'True change point'),
col = c('black', 'red', 'green', 'blue'),
lty = c(1,2,3,4), cex = 1.2, bty = 'n')
i = 30
N = nt = 50; x = (0:N)/N; nw = N+1
r_thin = seq(iters*3/4+1, iters, thin_step)
x <- seq(0,1,0.02)
library(latex2exp)
mean_try = rep(alpha.mean, nw)*x*(1-x)+(ifelse(0<rep(p.mean[i],nw),1,0))*
(ifelse(x<=rep(c.mean[i],nw),1,0))*N*x^2*rep(tau.mean[i],nw)*(1-rep(c.mean[i],nw))^2+
(ifelse(0<rep(p.mean[i],nw),1,0))*(ifelse(x>rep(c.mean[i],nw),1,0))*N*
(1-x)^2*rep(tau.mean[i],nw)*rep(c.mean[i],nw)^2
plot(0:N, S_sim[,i], xlab = "time", ylab = TeX(r'($Y_{T,k}$)'), type = "l", ylim = c(0, max(S_sim[,i], mean_try)),
lty = 1, lwd = 2)
lines(0:N, mean_try, col = "red", lty = 2, lwd = 2)
abline(v = c.mean[i]*N, col = "red", lty = 2, lwd = 2)
possible_change = seq(0, 1, 0.01)
possible_beta = seq(-1, -40, by = -0.01)
result_linear = matrix(0, ncol = 3, nrow = length(possible_beta)*length(possible_change))
colnames(result_linear) = c("squared error", "cp", "slope")
index = 1
for(ci in possible_change){
for(betai in possible_beta){
result_linear[index, 2:3] = c(ci, betai)
mean = betai*((ci-1)*x0+(x0-ci)*(ifelse(x0>ci,1,0)))
result_linear[index, 1] = sum((S_sim[, i] - mean)^2)
index = index + 1
}
}
cp_linear = result_linear[which.min(result_linear[, 1]), ][2]
beta_linear = result_linear[which.min(result_linear[, 1]), ][3]
x <- seq(0,1,0.02)
m_red = beta_linear*((cp_linear-1)*x+(x-cp_linear)*(ifelse(x>cp_linear,1,0)))
lines(0:N, m_red, col = 'green', lty = 3, lwd = 2)
abline(v = cp_linear*N, col = "green", lty = 3, lwd = 2)
abline(v = k_star[i]*N, col = "blue", lty = 4, lwd = 2)
legend('topright', legend = c(TeX(r'($Y_{T,k}$)'), 'Quadratic Fit', 'Linear Fit',
'True change point'),
col = c('black', 'red', 'green', 'blue'),
lty = c(1,2,3,4), cex = 1.1, bty = 'n', lwd = 2)
i = 30
N = nt = 50; x = (0:N)/N; nw = N+1
r_thin = seq(iters*3/4+1, iters, thin_step)
x <- seq(0,1,0.02)
library(latex2exp)
mean_try = rep(alpha.mean, nw)*x*(1-x)+(ifelse(0<rep(p.mean[i],nw),1,0))*
(ifelse(x<=rep(c.mean[i],nw),1,0))*N*x^2*rep(tau.mean[i],nw)*(1-rep(c.mean[i],nw))^2+
(ifelse(0<rep(p.mean[i],nw),1,0))*(ifelse(x>rep(c.mean[i],nw),1,0))*N*
(1-x)^2*rep(tau.mean[i],nw)*rep(c.mean[i],nw)^2
plot(0:N, S_sim[,i], xlab = "time", ylab = TeX(r'($Y_{T,k}$)'), type = "l", ylim = c(0, max(S_sim[,i], mean_try)),
lty = 1, lwd = 2)
lines(0:N, mean_try, col = "red", lty = 2, lwd = 2)
abline(v = c.mean[i]*N, col = "red", lty = 2, lwd = 2)
possible_change = seq(0, 1, 0.01)
possible_beta = seq(-1, -40, by = -0.01)
result_linear = matrix(0, ncol = 3, nrow = length(possible_beta)*length(possible_change))
colnames(result_linear) = c("squared error", "cp", "slope")
index = 1
for(ci in possible_change){
for(betai in possible_beta){
result_linear[index, 2:3] = c(ci, betai)
mean = betai*((ci-1)*x0+(x0-ci)*(ifelse(x0>ci,1,0)))
result_linear[index, 1] = sum((S_sim[, i] - mean)^2)
index = index + 1
}
}
cp_linear = result_linear[which.min(result_linear[, 1]), ][2]
beta_linear = result_linear[which.min(result_linear[, 1]), ][3]
x <- seq(0,1,0.02)
m_red = beta_linear*((cp_linear-1)*x+(x-cp_linear)*(ifelse(x>cp_linear,1,0)))
lines(0:N, m_red, col = 'green', lty = 3, lwd = 2)
abline(v = cp_linear*N, col = "green", lty = 3, lwd = 2)
abline(v = k_star[i]*N, col = "blue", lty = 4, lwd = 2)
legend('topright', legend = c(TeX(r'($Y_{T,k}$)'), 'Quadratic Fit', 'Linear Fit',
'True Change Point'),
col = c('black', 'red', 'green', 'blue'),
lty = c(1,2,3,4), cex = 1.1, bty = 'n', lwd = 2)
